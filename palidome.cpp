#include <stdio.h>
#include <string.h>





int main(){
char string[1000],temp[1000]; 
printf("Enter your the string of you want to check if its a palidome \n");
scanf("%s",string); 


strcpy(temp[1000],strrev(string)));
if (temp==string){printf("the string %s is a palidome",string);
}
else{printf("the string %s is not a palidome",string);
}

return 0;

}
